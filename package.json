{
  "name": "svrx-plugin-{{name}}",
  "version": "{{version}}",
  "description": "{{name}} plugin for server-x",
  "main": "index.js",
  "scripts": {},
  "engines": {
    "svrx": "^{{svrxVersion}}"
  },
  "keywords": [
    "svrx",
    "svrx-plugin",
    "{{name}}"
  ],
  "author": "{{author}}",
  "license": "{{license}}",
  "devDependencies": {},
  "dependencies": {}
}